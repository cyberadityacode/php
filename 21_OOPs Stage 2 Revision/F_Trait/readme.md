In PHP, a Trait is a mechanism for code reuse.

PHP doesn’t support multiple inheritance (a class can extend only one class).

Traits solve this by allowing you to inject reusable methods into multiple classes.

Traits can have methods and properties.

A class can use multiple traits at once.

-- Think of Traits as “horizontal reuse”, while inheritance is “vertical reuse.”